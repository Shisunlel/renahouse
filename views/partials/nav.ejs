<nav class="navbar navbar-light bg-light-shaded border-bottom">
  <a class="navbar-brand" href="/"
    ><img class="w-100" src="/img/logo.png" alt=""
  /></a>
  <div id="form-container">
    <form class="form-inline my-lg-0 justify-content-end">
      <input
        id="navSearch"
        class="form-control mr-md-2 rounded"
        type="search"
        name="q"
        placeholder="Search"
        aria-label="Search"
      />
      <button
        class="btn btn-dark-shade d-none d-md-block text-white"
        type="submit"
      >
        Search
      </button>
    </form>
  </div>
  <button
    class="navbar-toggler d-none d-md-block rounded"
    type="button"
    data-toggle="collapse"
    data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
    <span id="navProfile">
      <img
        class="w-100"
        src="https://icon-library.com/images/no-user-image-icon/no-user-image-icon-26.jpg"
        alt=""
      />
    </span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <% if(!currentUser){ %>
      <li class="nav-item">
        <a class="nav-link" href="/register">Register</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/login">Login</a>
      </li>
      <hr width="100%" style="background: rgba(255, 255, 255, 0.5)" />
      <% }else{ %>
      <li class="nav-item">
        <span class="navbar-text disabled"
          >Logged In As, <%= currentUser.username.toUpperCase() %></span
        >
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/logout">Log Out</a>
      </li>
      <hr width="100%" style="background: rgba(255, 255, 255, 0.5)" />
      <% } %>
      <li class="nav-item">
        <a class="nav-link" href="/house/new">Become a host</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/help">Help</a>
      </li>
    </ul>
  </div>
</nav>
<div class="container">
  <% if(error.length > 0){ %>
  <div class="alert alert-danger" role="alert"><%= error %></div>
  <% } %> <% if(success.length > 0){ %>
  <div class="alert alert-success" role="alert"><%= success %></div>
  <% } %>
</div>
